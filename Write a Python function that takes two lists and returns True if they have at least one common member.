

# Write a Python function that takes two lists and returns True if they have at least one common member.
lst = list(input("ENTER FIRST LIST--"))
lst1 = list(input("ENTER SECOND LIST--"))
lst2 = []
for i in lst:

    if (i in lst1) is True:
        lst2.append(i)

print("Common Member from both lists are--:", lst2)
